import { createMuiTheme } from '@material-ui/core/styles';

const snake_irl_theme = createMuiTheme({
  typography: {
    fontFamily: ['DotGothic16', 'sans-serif'].join(','),
  },
});

export default snake_irl_theme;
